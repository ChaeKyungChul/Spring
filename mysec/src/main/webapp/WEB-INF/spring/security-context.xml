<?xml version="1.0" encoding="UTF-8"?> <!--//bean 설정 -->
<b:beans xmlns="http://www.springframework.org/schema/security"
		 xmlns:b="http://www.springframework.org/schema/beans"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://www.springframework.org/schema/beans
		                     https://www.springframework.org/schema/beans/spring-beans.xsd
						     http://www.springframework.org/schema/security 
						     https://www.springframework.org/schema/security/spring-security.xsd">


     <!-- 사용자 설정 -->
	<authentication-manager>
	   <authentication-provider>
	     <user-service>
	       <user name="admin" password="{noop}admin123" authorities="ROLE_ADMIN,ROLE_USER"/>
	       <user name="user" password="{noop}user123" authorities="ROLE_USER" /> 
	     </user-service>
	   </authentication-provider>
	</authentication-manager>

     <!-- 폴더권한 -->
     <http auto-config="true" use-expressions="true">  
     <!-- auto-config 자동 등록 옵션, 요즘은 사용하지않음 -->
     <!-- use-expression 은 보안설정 -->
       <intercept-url pattern="/admin/**" access="hasRole('ADMIN')" />
       <intercept-url pattern="/user/**" access="hasRole('USER')" />
       <intercept-url pattern="/**" access="permitAll" />
       <form-login />
       <csrf disabled="true" />     
     </http>
	

</b:beans>